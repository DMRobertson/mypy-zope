import errno


def untilConcludes(f, *a, **kw):
    while True:
        try:
            return f(*a, **kw)
        except OSError as e:
            if e.args[0] == errno.EINTR:
                continue
            raise


__all__ = [
    "untilConcludes",
]
